server:
 port: 5151
 compression:
   enabled: true
   mime-types: application/json,application/xml
   min-response-size: 500
 context-path: /v1

spring:
  data:
    mongodb:
        uri: mongodb://127.0.0.1:27017
        database: som
  http:
    encoding:
      enabled: true
      charset: utf-8