server:
 port: 5151
 compression:
   enabled: true
   mime-types: application/json,application/xml
   min-response-size: 500
 context-path: /v1

spring:
  data:
    mongodb:
      database: som
  http:
    encoding:
      enabled: true
      charset: utf-8